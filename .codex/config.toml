# Project-scoped permissions for the create-line-stickers workflow.
# Codex loads this file only when this repository is trusted.
approval_policy = "on-request"
sandbox_mode = "workspace-write"
web_search = "live"

[sandbox_workspace_write]
network_access = true
